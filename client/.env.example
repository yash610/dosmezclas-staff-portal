# URL of the deployed backend API (no trailing slash).
# Leave blank in local dev — Vite proxies /api to localhost:4000 automatically.
# In production set this to your Vercel API deployment URL.
VITE_API_URL=https://your-api.vercel.app
